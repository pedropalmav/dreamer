[project]
name = "dreamer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "cookie-env",
]

[tool.uv.sources]
cookie-env = { git = "https://github.com/pedropalmav/cookie-env" }
